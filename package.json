{
  "name": "dsh-cursor-codex",
  "version": "0.1.0",
  "private": true,
  "description": "Connect DeepSeek Harness (dsh) to Cursor and Codex: native model provider, ACP agent bundle, MCP server, skills, and config templates.",
  "license": "MIT",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "test:gateway": "node --test gateway/local-gateway.test.mjs",
    "gateway:doctor": "node gateway/local-gateway.mjs doctor"
  },
  "keywords": [
    "deepseek",
    "deepseek-harness",
    "dsh",
    "dsh-plugin",
    "cursor",
    "codex",
    "acp",
    "mcp"
  ]
}
